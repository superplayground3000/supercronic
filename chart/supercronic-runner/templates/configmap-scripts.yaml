apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "supercronic.fullname" . }}-scripts
data:
{{- range $name, $content := .Values.scripts }}
  {{ $name }}: |
{{ $content | indent 4 }}
{{- end }}
